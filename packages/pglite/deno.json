{
  "name": "@fedify/pglite",
  "version": "2.4.0",
  "license": "MIT",
  "exports": {
    ".": "./src/mod.ts",
    "./kv": "./src/kv.ts"
  },
  "exclude": [
    "dist",
    "node_modules"
  ],
  "publish": {
    "exclude": [
      "**/*.test.ts",
      "tsdown.config.ts"
    ]
  },
  "tasks": {
    "check": "deno fmt --check && deno lint && deno check src/*.ts",
    "test": "deno test --allow-read --allow-write --allow-env"
  }
}
